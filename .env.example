# ===================================================
# 環境変数のテンプレート（Git に含めてOK）
# クローン後にこのファイルをコピーして .env を作成してください
#   cp .env.example .env
# 開発環境では中身の編集は不要です。
# ===================================================

APP_HOST=localhost
