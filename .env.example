# ===================================================
# 環境変数のテンプレート（Git に含めてOK）
# クローン後にこのファイルをコピーして .env を作成してください
#   cp .env.example .env
# ===================================================

APP_HOST=localhost

# オーナー（発注依頼メールの宛先）
OWNER_EMAIL=owner@example.com

# 送信元メールアドレス（省略時は noreply@mendo-app.local）
# MAILER_FROM=noreply@mendo-app.local
