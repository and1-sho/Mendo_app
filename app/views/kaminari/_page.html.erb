<%# Link showing page number
  - available local variables
    page:          a page object for "this" page
    url:           url to this page
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
-%>
<% if page.current? %>
  <span class="flex items-center justify-center w-8 h-8 text-sm md:w-9 md:h-9 md:text-base mx-1 rounded-lg bg-accent text-white font-bold">
    <%= page %>
  </span>
<% else %>
  <%= link_to page, url,
      class: "flex items-center justify-center w-8 h-8 text-sm md:w-9 md:h-9 md:text-base mx-1 rounded-lg bg-white text-neutral-900",
    data: { turbo_frame: "items" } %>
<% end %>
